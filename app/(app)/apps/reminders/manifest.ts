import { AppManifest } from "../common"

export const manifest: AppManifest = {
  name: "Reminders",
  description: "Set reminders for tax deadlines, invoice due dates, and bookkeeping tasks",
  icon: "Bell",
}
